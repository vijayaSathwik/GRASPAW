# Simulation

5-DOF URDF for the GRASPAW arm (SO-101 derived, forearm link removed).

## View in RViz

    cd simulation
    ros2 launch urdf_tutorial display.launch.py model:=$PWD/so101_5dof_calib.urdf

Mesh paths are relative to this directory, so run the launch from inside `simulation/`.
